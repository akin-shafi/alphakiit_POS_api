// main.go (project root)
package posfiberapp

// This file exists only to define the main package at the module root.
// The real entry point is in cmd/server/main.go
// This allows `go run .` to work from root if needed, but actual server is in cmd/server.
